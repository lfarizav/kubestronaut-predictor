apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "house-price-predictor.fullname" . }}-test-connection"
  labels:
    {{- include "house-price-predictor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "house-price-predictor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
