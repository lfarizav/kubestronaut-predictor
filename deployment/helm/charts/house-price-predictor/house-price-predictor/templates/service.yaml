apiVersion: v1
kind: Service
metadata:
  name: {{ include "house-price-predictor.fullname" . }}
  labels:
    {{- include "house-price-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "house-price-predictor.selectorLabels" . | nindent 4 }}
